{
  "properties": {
    "role": {
      "type": "string",
      "description": "The role of the user in the app",
      "enum": [
        "admin",
        "user"
      ]
    },
    "subscription_tier": {
      "type": "string",
      "description": "Subscription plan \u2014 controls access to premium features like ElevenLabs voiceovers",
      "enum": [
        "free",
        "pro"
      ],
      "default": "free"
    }
  },
  "required": [
    "role"
  ],
  "name": "User",
  "type": "object"
}